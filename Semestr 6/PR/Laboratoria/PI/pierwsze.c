#include <stdio.h>
#include <math.h>
#include <omp.h>
#include <stdlib.h>

const int N = 10000000;

int main() {
    int SQRT_N = (int)sqrt(N);
    long int temp[SQRT_N + 1];
    long int primes[N/10];
    long int num, remain;
    long int divisorCount;
    long int primeCount = 0;
    

}

